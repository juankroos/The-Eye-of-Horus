  schemas:
    payment_methods:
      type: object
      properties:

    Currency:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          example: "John Cena"
        code:
          type: string
        symbol:
          type: string
        countries:
          type: array
          items:
        is_default:
          type: boolean
          example: false
        can_send:
          type: boolean
          example: true
        can_receive:
          type: boolean
          example: false
      required:
        - id
        - name
        - code
        - symbol
    Country:
      type: object
      properties:
        id:
          type: string
          format: uuid
        region_id:
          type: string
          format: uuid
        name:
          type: string
          example: 'france'
        iso_3166_2:
          type: string
          example: 'CM'
        iso_3166_3:
          type: string
          example: 'CMR'
        calling_code:
          type: string
        is_active:
          type: boolean
          example: 'true'
        currencies:
          type: array
          items:
        payment_methods:
          type: array
          items:
        region:
          type: object

      required:
        - id
        - region_id
        - name
        - iso_3166_2
        - iso_3166_3
        - calling_code
        - is_active
    Region:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        code:
          type: string
        countries:
          type: array
          items:

      required:
        - id
        - name
        - code
  securitySchemes:
    default:
      type: http
      scheme: bearer
      description: 'You can retrieve your token by using the login endpoints of the IAM microservice.'
security:
  -
    default: []
  securitySchemes:
    default:
      type: http
      scheme: bearer
      description: 'You can retrieve your token by using the login endpoints of the IAM microservice.'
security:
  - default: []
  schemas:
    Currency:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          example: "John Cena"
        code:
          type: string
        symbol:
          type: string
        countries:
          type: array
        is_default:
          type: boolean
          example: false
        can_send:
          type: boolean
          example: true
        can_receive:
          type: boolean
          example: false
      required:
        - id
        - name
        - code
        - symbol
    Country:
      type: object
      properties:
        id:
          type: string
          format: uuid
        region_id:
          type: string
          format: uuid
        name:
          type: string
        iso_3166_2:
          type: string
        iso_3166_3:
          type: string
        calling_code:
          type: string
        currency:
      required:
        - id
        - region_id
        - name
        - iso_3166_2
        - iso_3166_3
        - calling_code
    Region:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        code:
          type: string
        countries:
          type: array

      required:
        - id
        - name
        - code

    SetProfilePhone:
      type: object
      properties:
        flow_key:
          type: string
          description: 'The flow key generated from the `initiate` step.'
          example: 01K41FZW85V9T4JM4GES03355Q
        first_name:
          type: string
          maxLength: 255
          description: "The customer's first name."
          example: 'John'
        last_name:
          type: string
          maxLength: 255
          description: "The customer's last name"
          example: 'Doe'
        phone_number:
          type: string
          maxLength: 32
          description: "Customer's phone number. This field is required when `email` is not present. Must not be greater than 32 characters."
          example: 237699123456
        send_mode:
          type: string
          description: 'How to receive the OTP code. This field is required when `phone_number` is present.'
          example: SMS
          enum:
            - SMS
            - WHATSAPP
      required:
          - flow_key
          - first_name
          - last_name
          - phone_number

    SetProfileEmail:
      type: object
      properties:
        flow_key:
          type: string
          description: 'The flow key generated from the `initiate` step.'
          example: 01K41FZW85V9T4JM4GES03355Q
        first_name:
          type: string
          maxLength: 255
          description: "The customer's first name."
          example: 'John'
        last_name:
          type: string
          maxLength: 255
          description: "The customer's last name"
          example: 'Doe'
        email:
          type: string
          formal: email
          description: 'This field is required when <code>phone_number</code> is not present. Must be a valid email address. Must not be greater than 255 characters.'
          example: rempel.chadrick@example.org
      required:
          - flow_key
          - first_name
          - last_name
          - email

    Customer:
      type: object
      properties:
        id:
          type: string
        country_id:
          type: string
          format: uuid
          example:
        locale:
          type: string
          enum:
            - en
        email_verified_at:
          type: date-time










  schemas:
    payment_methods:
      type: object
      properties:

    Currency:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          example: "John Cena"
        code:
          type: string
        symbol:
          type: string
        countries:
          type: array
          items:
        is_default:
          type: boolean
          example: false
        can_send:
          type: boolean
          example: true
        can_receive:
          type: boolean
          example: false
      required:
        - id
        - name
        - code
        - symbol
    Country:
      type: object
      properties:
        id:
          type: string
          format: uuid
        region_id:
          type: string
          format: uuid
        name:
          type: string
          example: 'france'
        iso_3166_2:
          type: string
          example: 'CM'
        iso_3166_3:
          type: string
          example: 'CMR'
        calling_code:
          type: string
        is_active:
          type: boolean
          example: 'true'
        currencies:
          type: array
          items:
        payment_methods:
          type: array
          items:
        region:
          type: object

      required:
        - id
        - region_id
        - name
        - iso_3166_2
        - iso_3166_3
        - calling_code
        - is_active
    Region:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        code:
          type: string
        countries:
          type: array
          items:

      required:
        - id
        - name
        - code
  securitySchemes:
    default:
      type: http
      scheme: bearer
      description: 'You can retrieve your token by using the login endpoints of the IAM microservice.'
security:
  -
    default: []
  securitySchemes:
    default:
      type: http
      scheme: bearer
      description: 'You can retrieve your token by using the login endpoints of the IAM microservice.'
security:
  - default: []
  schemas:
    Currency:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          example: "John Cena"
        code:
          type: string
        symbol:
          type: string
        countries:
          type: array
        is_default:
          type: boolean
          example: false
        can_send:
          type: boolean
          example: true
        can_receive:
          type: boolean
          example: false
      required:
        - id
        - name
        - code
        - symbol
    Country:
      type: object
      properties:
        id:
          type: string
          format: uuid
        region_id:
          type: string
          format: uuid
        name:
          type: string
        iso_3166_2:
          type: string
        iso_3166_3:
          type: string
        calling_code:
          type: string
        currency:
      required:
        - id
        - region_id
        - name
        - iso_3166_2
        - iso_3166_3
        - calling_code
    Region:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        code:
          type: string
        countries:
          type: array

      required:
        - id
        - name
        - code

    SetProfilePhone:
      type: object
      properties:
        flow_key:
          type: string
          description: 'The flow key generated from the `initiate` step.'
          example: 01K41FZW85V9T4JM4GES03355Q
        first_name:
          type: string
          maxLength: 255
          description: "The customer's first name."
          example: 'John'
        last_name:
          type: string
          maxLength: 255
          description: "The customer's last name"
          example: 'Doe'
        phone_number:
          type: string
          maxLength: 32
          description: "Customer's phone number. This field is required when `email` is not present. Must not be greater than 32 characters."
          example: 237699123456
        send_mode:
          type: string
          description: 'How to receive the OTP code. This field is required when `phone_number` is present.'
          example: SMS
          enum:
            - SMS
            - WHATSAPP
      required:
          - flow_key
          - first_name
          - last_name
          - phone_number

    SetProfileEmail:
      type: object
      properties:
        flow_key:
          type: string
          description: 'The flow key generated from the `initiate` step.'
          example: 01K41FZW85V9T4JM4GES03355Q
        first_name:
          type: string
          maxLength: 255
          description: "The customer's first name."
          example: 'John'
        last_name:
          type: string
          maxLength: 255
          description: "The customer's last name"
          example: 'Doe'
        email:
          type: string
          formal: email
          description: 'This field is required when <code>phone_number</code> is not present. Must be a valid email address. Must not be greater than 255 characters.'
          example: rempel.chadrick@example.org
      required:
          - flow_key
          - first_name
          - last_name
          - email

    Customer:
      type: object
      properties:
        id:
          type: string
        country_id:
          type: string
          format: uuid
          example:
        locale:
          type: string
          enum:
            - en
        email_verified_at:
          type: date-time










  schemas:
    payment_methods:
      type: object
      properties:

    Currency:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          example: "John Cena"
        code:
          type: string
        symbol:
          type: string
        countries:
          type: array
          items:
        is_default:
          type: boolean
          example: false
        can_send:
          type: boolean
          example: true
        can_receive:
          type: boolean
          example: false
      required:
        - id
        - name
        - code
        - symbol
    Country:
      type: object
      properties:
        id:
          type: string
          format: uuid
        region_id:
          type: string
          format: uuid
        name:
          type: string
          example: 'france'
        iso_3166_2:
          type: string
          example: 'CM'
        iso_3166_3:
          type: string
          example: 'CMR'
        calling_code:
          type: string
        is_active:
          type: boolean
          example: 'true'
        currencies:
          type: array
          items:
        payment_methods:
          type: array
          items:
        region:
          type: object

      required:
        - id
        - region_id
        - name
        - iso_3166_2
        - iso_3166_3
        - calling_code
        - is_active
    Region:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        code:
          type: string
        countries:
          type: array
          items:

      required:
        - id
        - name
        - code
  securitySchemes:
    default:
      type: http
      scheme: bearer
      description: 'You can retrieve your token by using the login endpoints of the IAM microservice.'
security:
  -
    default: []
  securitySchemes:
    default:
      type: http
      scheme: bearer
      description: 'You can retrieve your token by using the login endpoints of the IAM microservice.'
security:
  - default: []
  schemas:
    Currency:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          example: "John Cena"
        code:
          type: string
        symbol:
          type: string
        countries:
          type: array
        is_default:
          type: boolean
          example: false
        can_send:
          type: boolean
          example: true
        can_receive:
          type: boolean
          example: false
      required:
        - id
        - name
        - code
        - symbol
    Country:
      type: object
      properties:
        id:
          type: string
          format: uuid
        region_id:
          type: string
          format: uuid
        name:
          type: string
        iso_3166_2:
          type: string
        iso_3166_3:
          type: string
        calling_code:
          type: string
        currency:
      required:
        - id
        - region_id
        - name
        - iso_3166_2
        - iso_3166_3
        - calling_code
    Region:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        code:
          type: string
        countries:
          type: array

      required:
        - id
        - name
        - code

    SetProfilePhone:
      type: object
      properties:
        flow_key:
          type: string
          description: 'The flow key generated from the `initiate` step.'
          example: 01K41FZW85V9T4JM4GES03355Q
        first_name:
          type: string
          maxLength: 255
          description: "The customer's first name."
          example: 'John'
        last_name:
          type: string
          maxLength: 255
          description: "The customer's last name"
          example: 'Doe'
        phone_number:
          type: string
          maxLength: 32
          description: "Customer's phone number. This field is required when `email` is not present. Must not be greater than 32 characters."
          example: 237699123456
        send_mode:
          type: string
          description: 'How to receive the OTP code. This field is required when `phone_number` is present.'
          example: SMS
          enum:
            - SMS
            - WHATSAPP
      required:
          - flow_key
          - first_name
          - last_name
          - phone_number

    SetProfileEmail:
      type: object
      properties:
        flow_key:
          type: string
          description: 'The flow key generated from the `initiate` step.'
          example: 01K41FZW85V9T4JM4GES03355Q
        first_name:
          type: string
          maxLength: 255
          description: "The customer's first name."
          example: 'John'
        last_name:
          type: string
          maxLength: 255
          description: "The customer's last name"
          example: 'Doe'
        email:
          type: string
          formal: email
          description: 'This field is required when <code>phone_number</code> is not present. Must be a valid email address. Must not be greater than 255 characters.'
          example: rempel.chadrick@example.org
      required:
          - flow_key
          - first_name
          - last_name
          - email

    Customer:
      type: object
      properties:
        id:
          type: string
        country_id:
          type: string
          format: uuid
          example:
        locale:
          type: string
          enum:
            - en
        email_verified_at:
          type: date-time










  schemas:
    payment_methods:
      type: object
      properties:

    Currency:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          example: "John Cena"
        code:
          type: string
        symbol:
          type: string
        countries:
          type: array
          items:
        is_default:
          type: boolean
          example: false
        can_send:
          type: boolean
          example: true
        can_receive:
          type: boolean
          example: false
      required:
        - id
        - name
        - code
        - symbol
    Country:
      type: object
      properties:
        id:
          type: string
          format: uuid
        region_id:
          type: string
          format: uuid
        name:
          type: string
          example: 'france'
        iso_3166_2:
          type: string
          example: 'CM'
        iso_3166_3:
          type: string
          example: 'CMR'
        calling_code:
          type: string
        is_active:
          type: boolean
          example: 'true'
        currencies:
          type: array
          items:
        payment_methods:
          type: array
          items:
        region:
          type: object

      required:
        - id
        - region_id
        - name
        - iso_3166_2
        - iso_3166_3
        - calling_code
        - is_active
    Region:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        code:
          type: string
        countries:
          type: array
          items:

      required:
        - id
        - name
        - code
  securitySchemes:
    default:
      type: http
      scheme: bearer
      description: 'You can retrieve your token by using the login endpoints of the IAM microservice.'
security:
  -
    default: []
  securitySchemes:
    default:
      type: http
      scheme: bearer
      description: 'You can retrieve your token by using the login endpoints of the IAM microservice.'
security:
  - default: []
  schemas:
    Currency:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          example: "John Cena"
        code:
          type: string
        symbol:
          type: string
        countries:
          type: array
        is_default:
          type: boolean
          example: false
        can_send:
          type: boolean
          example: true
        can_receive:
          type: boolean
          example: false
      required:
        - id
        - name
        - code
        - symbol
    Country:
      type: object
      properties:
        id:
          type: string
          format: uuid
        region_id:
          type: string
          format: uuid
        name:
          type: string
        iso_3166_2:
          type: string
        iso_3166_3:
          type: string
        calling_code:
          type: string
        currency:
      required:
        - id
        - region_id
        - name
        - iso_3166_2
        - iso_3166_3
        - calling_code
    Region:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        code:
          type: string
        countries:
          type: array

      required:
        - id
        - name
        - code

    SetProfilePhone:
      type: object
      properties:
        flow_key:
          type: string
          description: 'The flow key generated from the `initiate` step.'
          example: 01K41FZW85V9T4JM4GES03355Q
        first_name:
          type: string
          maxLength: 255
          description: "The customer's first name."
          example: 'John'
        last_name:
          type: string
          maxLength: 255
          description: "The customer's last name"
          example: 'Doe'
        phone_number:
          type: string
          maxLength: 32
          description: "Customer's phone number. This field is required when `email` is not present. Must not be greater than 32 characters."
          example: 237699123456
        send_mode:
          type: string
          description: 'How to receive the OTP code. This field is required when `phone_number` is present.'
          example: SMS
          enum:
            - SMS
            - WHATSAPP
      required:
          - flow_key
          - first_name
          - last_name
          - phone_number

    SetProfileEmail:
      type: object
      properties:
        flow_key:
          type: string
          description: 'The flow key generated from the `initiate` step.'
          example: 01K41FZW85V9T4JM4GES03355Q
        first_name:
          type: string
          maxLength: 255
          description: "The customer's first name."
          example: 'John'
        last_name:
          type: string
          maxLength: 255
          description: "The customer's last name"
          example: 'Doe'
        email:
          type: string
          formal: email
          description: 'This field is required when <code>phone_number</code> is not present. Must be a valid email address. Must not be greater than 255 characters.'
          example: rempel.chadrick@example.org
      required:
          - flow_key
          - first_name
          - last_name
          - email

    Customer:
      type: object
      properties:
        id:
          type: string
        country_id:
          type: string
          format: uuid
          example:
        locale:
          type: string
          enum:
            - en
        email_verified_at:
          type: date-time










  schemas:
    payment_methods:
      type: object
      properties:

    Currency:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          example: "John Cena"
        code:
          type: string
        symbol:
          type: string
        countries:
          type: array
          items:
        is_default:
          type: boolean
          example: false
        can_send:
          type: boolean
          example: true
        can_receive:
          type: boolean
          example: false
      required:
        - id
        - name
        - code
        - symbol
    Country:
      type: object
      properties:
        id:
          type: string
          format: uuid
        region_id:
          type: string
          format: uuid
        name:
          type: string
          example: 'france'
        iso_3166_2:
          type: string
          example: 'CM'
        iso_3166_3:
          type: string
          example: 'CMR'
        calling_code:
          type: string
        is_active:
          type: boolean
          example: 'true'
        currencies:
          type: array
          items:
        payment_methods:
          type: array
          items:
        region:
          type: object

      required:
        - id
        - region_id
        - name
        - iso_3166_2
        - iso_3166_3
        - calling_code
        - is_active
    Region:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        code:
          type: string
        countries:
          type: array
          items:

      required:
        - id
        - name
        - code
  securitySchemes:
    default:
      type: http
      scheme: bearer
      description: 'You can retrieve your token by using the login endpoints of the IAM microservice.'
security:
  -
    default: []
  securitySchemes:
    default:
      type: http
      scheme: bearer
      description: 'You can retrieve your token by using the login endpoints of the IAM microservice.'
security:
  - default: []
  schemas:
    Currency:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          example: "John Cena"
        code:
          type: string
        symbol:
          type: string
        countries:
          type: array
        is_default:
          type: boolean
          example: false
        can_send:
          type: boolean
          example: true
        can_receive:
          type: boolean
          example: false
      required:
        - id
        - name
        - code
        - symbol
    Country:
      type: object
      properties:
        id:
          type: string
          format: uuid
        region_id:
          type: string
          format: uuid
        name:
          type: string
        iso_3166_2:
          type: string
        iso_3166_3:
          type: string
        calling_code:
          type: string
        currency:
      required:
        - id
        - region_id
        - name
        - iso_3166_2
        - iso_3166_3
        - calling_code
    Region:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        code:
          type: string
        countries:
          type: array

      required:
        - id
        - name
        - code

    SetProfilePhone:
      type: object
      properties:
        flow_key:
          type: string
          description: 'The flow key generated from the `initiate` step.'
          example: 01K41FZW85V9T4JM4GES03355Q
        first_name:
          type: string
          maxLength: 255
          description: "The customer's first name."
          example: 'John'
        last_name:
          type: string
          maxLength: 255
          description: "The customer's last name"
          example: 'Doe'
        phone_number:
          type: string
          maxLength: 32
          description: "Customer's phone number. This field is required when `email` is not present. Must not be greater than 32 characters."
          example: 237699123456
        send_mode:
          type: string
          description: 'How to receive the OTP code. This field is required when `phone_number` is present.'
          example: SMS
          enum:
            - SMS
            - WHATSAPP
      required:
          - flow_key
          - first_name
          - last_name
          - phone_number

    SetProfileEmail:
      type: object
      properties:
        flow_key:
          type: string
          description: 'The flow key generated from the `initiate` step.'
          example: 01K41FZW85V9T4JM4GES03355Q
        first_name:
          type: string
          maxLength: 255
          description: "The customer's first name."
          example: 'John'
        last_name:
          type: string
          maxLength: 255
          description: "The customer's last name"
          example: 'Doe'
        email:
          type: string
          formal: email
          description: 'This field is required when <code>phone_number</code> is not present. Must be a valid email address. Must not be greater than 255 characters.'
          example: rempel.chadrick@example.org
      required:
          - flow_key
          - first_name
          - last_name
          - email

    Customer:
      type: object
      properties:
        id:
          type: string
        country_id:
          type: string
          format: uuid
          example:
        locale:
          type: string
          enum:
            - en
        email_verified_at:
          type: date-time










components:
  schemas:
    payment_methods:
      type: object
      properties:

    Currency:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          example: "John Cena"
        code:
          type: string
        symbol:
          type: string
        countries:
          type: array
          items:
            $ref: "#/components/schemas/Country"
        is_default:
          type: boolean
          example: false
        can_send:
          type: boolean
          example: true
        can_receive:
          type: boolean
          example: false
      required:
        - id
        - name
        - code
        - symbol
    Country:
      type: object
      properties:
        id:
          type: string
          format: uuid
        region_id:
          type: string
          format: uuid
        name:
          type: string
          example: 'france'
        iso_3166_2:
          type: string
          example: 'CM'
        iso_3166_3:
          type: string
          example: 'CMR'
        calling_code:
          type: string
        is_active:
          type: boolean
          example: 'true'
        currencies:
          type: array
          items:
            $ref: "#/components/schemas/Currency"
        payment_methods:
          type: array
          items:
            $ref: "#/components/schemas/payment_methods"
        region:
          type: object
          $ref: "#/components/schemas/Region"

      required:
        - id
        - region_id
        - name
        - iso_3166_2
        - iso_3166_3
        - calling_code
        - is_active
    Region:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        code:
          type: string
        countries:
          type: array
          items:
            $ref: "#/components/schemas/Country"

      required:
        - id
        - name
        - code
  securitySchemes:
    default:
      type: http
      scheme: bearer
      description: 'You can retrieve your token by using the login endpoints of the IAM microservice.'
security:
  -
    default: []
paths:
components:
  securitySchemes:
    default:
      type: http
      scheme: bearer
      description: 'You can retrieve your token by using the login endpoints of the IAM microservice.'
security:
  - default: []
paths:
components:
  schemas:
    Currency:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          example: "John Cena"
        code:
          type: string
        symbol:
          type: string
        countries:
          type: array
          $ref: "#/components/schemas/Country"
        is_default:
          type: boolean
          example: false
        can_send:
          type: boolean
          example: true
        can_receive:
          type: boolean
          example: false
      required:
        - id
        - name
        - code
        - symbol
    Country:
      type: object
      properties:
        id:
          type: string
          format: uuid
        region_id:
          type: string
          format: uuid
        name:
          type: string
        iso_3166_2:
          type: string
        iso_3166_3:
          type: string
        calling_code:
          type: string
        currency:
          $ref: "#/components/schemas/Currency"
      required:
        - id
        - region_id
        - name
        - iso_3166_2
        - iso_3166_3
        - calling_code
    Region:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        code:
          type: string
        countries:
          type: array
          $ref: "#/components/schemas/Country"

      required:
        - id
        - name
        - code

    SetProfilePhone:
      type: object
      properties:
        flow_key:
          type: string
          description: 'The flow key generated from the `initiate` step.'
          example: 01K41FZW85V9T4JM4GES03355Q
        first_name:
          type: string
          maxLength: 255
          description: "The customer's first name."
          example: 'John'
        last_name:
          type: string
          maxLength: 255
          description: "The customer's last name"
          example: 'Doe'
        phone_number:
          type: string
          maxLength: 32
          description: "Customer's phone number. This field is required when `email` is not present. Must not be greater than 32 characters."
          example: 237699123456
        send_mode:
          type: string
          description: 'How to receive the OTP code. This field is required when `phone_number` is present.'
          example: SMS
          enum:
            - SMS
            - WHATSAPP
      required:
          - flow_key
          - first_name
          - last_name
          - phone_number

    SetProfileEmail:
      type: object
      properties:
        flow_key:
          type: string
          description: 'The flow key generated from the `initiate` step.'
          example: 01K41FZW85V9T4JM4GES03355Q
        first_name:
          type: string
          maxLength: 255
          description: "The customer's first name."
          example: 'John'
        last_name:
          type: string
          maxLength: 255
          description: "The customer's last name"
          example: 'Doe'
        email:
          type: string
          formal: email
          description: 'This field is required when <code>phone_number</code> is not present. Must be a valid email address. Must not be greater than 255 characters.'
          example: rempel.chadrick@example.org
      required:
          - flow_key
          - first_name
          - last_name
          - email

    Customer:
      type: object
      properties:
        id:
          type: string
        country_id:
          type: string
          format: uuid
          example:
        locale:
          type: string
          enum:
            - en
        email_verified_at:
          type: date-time










